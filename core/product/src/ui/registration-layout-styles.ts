/**
 * @deprecated Prefer `registrationChrome` / nested exports from `@inventory-platform/ui-kit`.
 * These are CSS class name strings (use with `className`, not `style`).
 */
export {
  registrationChrome,
  pageStyles,
  accordionStyles,
  vendorStyles,
  uploadLayoutStyles,
} from '@inventory-platform/ui-kit';
