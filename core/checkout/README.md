# @inventory-platform/checkout

**Status:** Scaffold only

Placeholder package from Phase 0. **Checkout UI and routes currently live in `core/product`** (`/dashboard/checkout`).

## Owns (today)

- Empty barrel + `queries/` stub only

## Next

Either:

1. Move checkout pages/API/queries from `core/product` into this package and wire routes/nav, or
2. Delete this package so the domain map stays honest.

Do not add new checkout features here until ownership is decided.
