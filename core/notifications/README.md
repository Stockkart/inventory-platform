# @inventory-platform/notifications

**Status:** Scaffold only

Placeholder package. **In-app notification bell, dropdown, and SSE/poll wiring live in `platform/shell`** (and related session/API helpers).

## Owns (today)

- Empty barrel + `queries/` stub only

## Next

Either migrate notification list/API ownership here (shell keeps chrome only), or delete this scaffold.

Shared DTO bits may live under `@inventory-platform/contracts` (`notification.types.ts`).
